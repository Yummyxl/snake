[project]
name = "chichi-backend"
version = "0.1.0"
description = "Backend API for chichi."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.100",
  "gymnasium>=0.28",
  "numpy>=1.26",
  "stable-baselines3>=2.0",
  "torch>=2.0",
  "uvicorn[standard]>=0.23",
]
